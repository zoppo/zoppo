(( $+3 )) || {
  warn "functions:autoload-file-relative: not enough arguments"
  return 1
}

functions:autoload-file "$1/$2" "${(@)argv[3,-1]}"

# vim: ft=zsh sts=2 ts=2 sw=2 et fdm=marker fmr={{{,}}}
